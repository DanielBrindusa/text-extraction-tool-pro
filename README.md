# text-extraction-tool-pro
Text Extraction Tool Pro is a modern web application that allows users to extract text between custom patterns from files or pasted content directly in the browser. It supports regex extraction, duplicate removal, sorting, previewing matches, and exporting results as TXT, CSV, or JSON — all with a clean modern interface and no installation required
